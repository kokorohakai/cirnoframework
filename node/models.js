var self = this;
this.build = function( cirno ){
    /***************************************************************************
    * build all models here. They are grouped by module, so please try to keep
    * it that way. Models are built the same way here as they are in PHP.
    * Models can be a little more, complete, than in PHP. Since we're dealing
    * With a create once and persisting environment. Plus the active state
    * of the server will allow it to pull and deliver this data much faster.
    ***************************************************************************/
    /* E.G:
    cirno.db.createModel('chat', 'chat', 'id', {
        "user_id": {
            "users" : "id"
        }  
    });
    */
}